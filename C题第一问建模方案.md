# C题第一问建模方案

## 1. 模型选择

第一问属于**优化问题**，建议建立“考虑储能充放电效率的微网日前经济调度模型”，采用**混合整数线性规划（MILP）**求解。

附件1已经给出一天的电价、小区负载和光伏预测功率，因此本问不需要另建光伏预测模型，重点是决定每个10分钟时段的：

- 外网购电量；
- 储能充电量；
- 储能放电量；
- 电池储电量变化。

按照讲义第38页的优化模型流程：

```text
确定决策变量
    ↓
建立目标函数
    ↓
列出约束条件
    ↓
判断模型类型并调用求解器
    ↓
约束检验、基准比较、敏感性分析
    ↓
整理表1、表2和result1.xlsx
```

若去掉充放电互斥变量，可以得到线性规划简化模型，但必须检查是否出现同一时段同时充电和放电。主方案建议保留互斥变量，物理含义更清楚。

## 2. 输入数据与口径

### 2.1 输入数据

| 输入 | 来源 | 含义 |
|---|---|---|
| 电价 (p_t) | 附件1 | 第 (t) 段购电价格，单位：元/kWh |
| 小区负载功率 | 附件1 | 单位：kW |
| 光伏预测功率 | 附件1 | 单位：kW |
| 电池容量 | 附录1 | 最大容量12000 kWh |
| 储电量上下限 | 附录1 | 1200–10800 kWh |
| 最大充放电功率 | 附录1 | 5000 kW |
| 初始储电量 | 附录1 | 6000 kWh |
| 充放电效率 | 附录1 | 90% |

### 2.2 时间和单位

一天划分为144个10分钟区间。若附件中的功率表示对应10分钟区间的平均功率，则：

\[
\text{区间电量}=\text{功率}\times\frac{1}{6}\text{小时}
\]

因此，负载和光伏功率必须先转换为kWh，再进入供需平衡约束。

结果模板存在时段标签疑点：模板从`0:10-0:20`开始，末段跨到次日。编码时应先建立“原始标签—标准0:00至24:00区间—模板单元格”的映射，不能简单按行号填入。

### 2.3 需要声明的解释假设

1. 暂按充电效率和放电效率各为90%处理。
2. 充放电量按微网侧电量计量。
3. 未提供售电价格，主方案不设置售电收入；多余光伏允许弃用。
4. 第一问采用附录中的6000 kWh作为日初电量，并令日末电量恢复为6000 kWh。

## 3. 决策变量

设 (t=1,2,\ldots,144)。

| 变量 | 含义 | 单位 |
|---|---|---|
| (q_t) | 第 (t) 段向外网购电量 | kWh |
| (c_t) | 第 (t) 段送入电池的充电量 | kWh |
| (d_t) | 第 (t) 段电池向微网输出的放电量 | kWh |
| (E_t) | 第 (t) 段结束后的储电量 | kWh |
| (s_t) | 无法利用而舍弃的光伏电量 | kWh |
| (u_t) | 充放电模式变量，取0或1 | 无量纲 |

其中，(u_t=1)表示允许充电，(u_t=0)表示允许放电或待机。

## 4. 目标函数与约束

设 (L_t) 为第 (t) 段负载电量，(V_t) 为第 (t) 段光伏预测电量。

### 4.1 目标函数

全天购电费用最小：

\[
\min C=\sum_{t=1}^{144}p_tq_t
\]

### 4.2 供需平衡

\[
q_t+V_t+d_t=L_t+c_t+s_t
\]

含义：外网购电、光伏发电和电池放电共同满足小区负载与电池充电需求。

### 4.3 储能状态递推

\[
E_t=E_{t-1}+0.9c_t-\frac{d_t}{0.9}
\]

含义：充电时只有 (0.9c_t) 进入电池；放电时，为向微网输出 (d_t)，电池内部需要消耗 (d_t/0.9)。

### 4.4 储电量上下限

\[
1200\le E_t\le10800
\]

### 4.5 充放电功率限制

10分钟为 (1/6) 小时，因此单段最大充放电量为 (5000/6) kWh：

\[
0\le c_t\le5000\times\frac{1}{6}u_t
\]

\[
0\le d_t\le5000\times\frac{1}{6}(1-u_t)
\]

### 4.6 非负与模式约束

\[
u_t\in\{0,1\},\qquad q_t\ge0,\qquad 0\le s_t\le V_t
\]

其中 (0\le s_t\le V_t) 表示只能弃用已有光伏，不能凭空产生弃光量。

### 4.7 日初日末约束

\[
E_0=6000,
\qquad E_{144}=E_0=6000
\]

日末恢复约束用于避免通过消耗日初库存来降低表面购电费用。

## 5. 五步实施流程

### 第1步：整理数据

**输入：**附件1、附录1、结果模板。

**处理：**

- 读取144个时间点；
- 检查时间类型、缺失值和跨日标签；
- 将负载功率和光伏功率转换为10分钟电量；
- 整理电池容量、功率、效率和初始电量。

**输出：**标准数据包 `DayData`、设备参数 `Battery` 和时间映射 `TimeMap`。

**接口：**

```python
prepare_inputs(attachment_path, config) -> DayData, Battery, TimeMap
```

### 第2步：建立优化模型

**输入：**标准数据包、设备参数和解释假设。

**处理：**

- 创建 (q_t,c_t,d_t,E_t,s_t,u_t)；
- 添加购电费最小目标；
- 添加供需平衡；
- 添加储能状态递推；
- 添加储电量上下限；
- 添加充放电功率限制；
- 添加充放电互斥和日初日末相等。

**输出：**优化模型 `model` 和变量字典 `variables`。

**接口：**

```python
build_model(data, config) -> model, variables
```

### 第3步：求解并提取方案

**输入：**优化模型和变量字典。

**处理：**

- 调用MILP求解器；
- 检查求解状态；
- 读取变量值；
- 保存目标函数值和求解状态；
- 如果求解器提供最优性差距，也一并保存。

**输出：**144段调度结果、145个储电边界状态、全天购电费用。

**接口：**

```python
solve_model(model, variables) -> result, status, solver_info
```

### 第4步：验证与比较

**输入：**调度结果、原始数据和统一参数。

**处理：**

- 逐段核对供需平衡；
- 检查储电量是否始终在1200–10800 kWh；
- 检查充放电量是否超过5000 kW对应的区间上限；
- 检查是否存在同时充放电；
- 检查 (E_0=E_{144}=6000)；
- 与“不使用储能”的基准方案比较；
- 设置效率或最大功率等参数情景，重新求解并比较费用。

**输出：**约束检查结果、基准费用对比、敏感性分析结果。

**接口：**

```python
validate_and_compare(data, result, config) -> checks, baseline, sensitivity
```

### 第5步：汇总和导出

**输入：**已核验结果、题目指定时间段和模板映射。

**处理：**

- 提取表1中的6个指定10分钟时段；
- 按0–4、4–8、8–12、12–16、16–20、20–24汇总表2；
- 计算全天购电量和全天购电费；
- 写入`result1.xlsx`；
- 检查汇总值与144段明细一致。

**输出：**表1、表2、`result1.xlsx`和绘图数据。

**接口：**

```python
summarize_outputs(data, result, checks, time_map) -> tables, workbook_data, plot_data
```

## 6. 应该绘制的图

以下图表只规划内容，不在本阶段生成。

### 图1：单日负载、光伏和电价曲线

- 上图：小区负载功率和光伏预测功率；
- 下图：电价；
- 横轴统一为0:00–24:00。

用途：展示供需差异和电价的分时特征。

### 图2：优化后的供能结构

建议使用堆叠柱形图或面积图，展示：

- 外网购电；
- 实际利用的光伏；
- 储能放电；
- 弃光量；
- 负载与储能充电需求。

用途：说明每个时段的负荷由哪些能源共同满足。

### 图3：储能运行状态

上图画充电和放电量：充电为正，放电为负；下图画储电量曲线，并标出：

- 1200 kWh下限；
- 10800 kWh上限；
- 6000 kWh日初和日末状态。

用途：展示储能的充放电时机和安全边界。

### 图4：无储能基准与优化方案比较

比较两种方案的：

- 全天购电费；
- 全天购电量。

购电费和购电量建议分成两个子图，避免把元和kWh放在同一坐标轴。

### 图5：参数敏感性分析

例如选择充放电效率：

- 横轴：效率情景，如85%、90%、95%；
- 纵轴：重新优化后的最低购电费用。

也可以分析最大充放电功率对购电费用和调度结构的影响。

注意：不能机械地把90%效率上调20%而得到超过100%的效率，应设置有物理意义的情景。

## 7. 代码总体结构

建议先按模块组织，不一次性编写全部代码：

```text
附件1和附录1
    ↓
prepare_inputs()
    ↓
标准化时间、单位和设备参数
    ↓
build_model()
    ↓
MILP模型
    ↓
solve_model()
    ↓
购电、充放电、储电量和费用
    ↓
validate_and_compare()
    ↓
约束检查、基准比较、敏感性分析
    ↓
summarize_outputs()
    ↓
表1、表2、result1.xlsx和绘图数据
```

代码骨架如下：

```python
def prepare_inputs(attachment_path, config):
    # TODO: 读取附件1
    # TODO: 统一时间标签并建立时间映射
    # TODO: 将kW转换为10分钟电量kWh
    # TODO: 返回DayData、Battery、TimeMap
    pass


def build_model(data, config):
    # TODO: 创建q[t], c[t], d[t], E[t], s[t], u[t]
    # TODO: 添加目标函数
    # TODO: 添加供需平衡和储能状态递推
    # TODO: 添加上下限、互斥和首尾相等
    pass


def solve_model(model, variables):
    # TODO: 调用MILP求解器
    # TODO: 检查status
    # TODO: 提取144段结果和145个储电状态
    pass


def validate_and_compare(data, result, config):
    # TODO: 独立复核所有约束
    # TODO: 计算无储能基准
    # TODO: 修改参数后重新建模和求解
    pass


def summarize_outputs(data, result, checks, time_map):
    # TODO: 提取表1和表2
    # TODO: 填写result1.xlsx
    # TODO: 生成绘图所需字段
    pass
```

结果明细建议统一保存以下字段：

```text
区间开始
区间结束
电价
负载电量
光伏预测电量
购电量
充电量
放电量
弃光量
期初储电量
期末储电量
本段购电费
```

## 8. 论文写作顺序

建议按以下顺序写第一问：

1. 说明附件1中的负载、光伏预测和电价特征；
2. 定义时间粒度、单位、变量和参数；
3. 给出购电费用目标函数；
4. 给出供需平衡、储能递推、功率限制、容量限制和首尾状态约束；
5. 说明采用MILP的原因；
6. 展示购电和储能调度图；
7. 检查模型约束和日初日末状态；
8. 与无储能基准比较；
9. 做效率或功率上限敏感性分析；
10. 按题目要求整理表1、表2及`result1.xlsx`。

## 9. 参考材料

- 赛题正文：`C题.pdf`第1页问题1和表1–2，第2页附录1，第3页`result1.xlsx`说明。
- 辅助讲义：`2026国赛零基础获奖速成课讲义.pdf`第38、42、75–76页。
- PuLP建模参考：<https://coin-or.github.io/pulp/CaseStudies/a_blending_problem.html>
- SciPy MILP参考：<https://docs.scipy.org/doc/scipy/reference/generated/scipy.optimize.milp.html>

本文件只给出第一问的建模和编码方案，不包含实际求解结果，也不直接填写结果模板。
